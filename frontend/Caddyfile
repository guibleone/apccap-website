:80 {
    root * /usr/share/caddy
    
    encode gzip zstd

    handle /api/* {
        reverse_proxy backend:3000
    }

    handle {
        try_files {path} {path}/ /index.html
        file_server
    }
}